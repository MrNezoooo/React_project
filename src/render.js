/*
import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from "./App";
import {addPost} from "./redux/state"; // експорт addPost
import {updateNewPostText} from "./redux/state"; // експорт updateNewPostText

//зєднує state.js
export let renderEntireTree = (state) => { //перемальвка всієї преложухі
    ReactDOM.render(  //Тут Відбуваєтся перерисовка всієї прілажухі
        <React.StrictMode>
            <App appState={state} addPostSecond={addPost} updateNewPostText={updateNewPostText} />
        </React.StrictMode>,
        document.getElementById('root'));
}*/
